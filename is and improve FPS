[1mdiff --git a/road_safety/.gitignore b/road_safety/.gitignore[m
[1mindex 12a08ab..4e40423 100644[m
[1m--- a/road_safety/.gitignore[m
[1m+++ b/road_safety/.gitignore[m
[36m@@ -37,7 +37,6 @@[m [mframes/[m
 # ---------------------------------------------------------------------------[m
 runs/[m
 datasets/pothole_runs/[m
[31m-datasets/flood_runs/[m
 wandb/[m
 [m
 # ---------------------------------------------------------------------------[m
[1mdiff --git a/road_safety/README.md b/road_safety/README.md[m
[1mindex 99b9ba4..243243e 100644[m
[1m--- a/road_safety/README.md[m
[1m+++ b/road_safety/README.md[m
[36m@@ -1,7 +1,7 @@[m
 # Road Safety Vision Pipeline[m
 [m
 Real-time webcam-based road hazard detection system.[m
[31m-Detects **lane position**, **potholes**, and **flooded roads** on a laptop GPU and sends compact results to an ESP32 over USB serial for V2V broadcasting.[m
[32m+[m[32mDetects **lane position** and **potholes** on a laptop GPU and sends compact results to an ESP32 over USB serial for V2V broadcasting.[m
 [m
 ---[m
 [m
[36m@@ -17,14 +17,14 @@[m [mWebcam (720p @ 15 FPS)[m
          │ raw BGR frames[m
     ┌────┴─────────────────────┐[m
     │                          │[m
[31m-┌───▼──────┐   ┌───────────────▼──────┐   ┌─────────────▼──────┐[m
[31m-│  lane    │   │   pothole            │   │   flood             │[m
[31m-│ detector │   │   detector           │   │   detector          │[m
[31m-│ (OpenCV) │   │   (YOLOv8-nano)      │   │   (MobileNetV2)    │[m
[31m-│          │   │   ROI: bottom 55%    │   │                     │[m
[31m-└───┬──────┘   └───────────┬──────────┘   └──────────┬──────────┘[m
[31m-    │                      │                          │[m
[31m-    └─────────────┬─────────┘──────────────────────────┘[m
[32m+[m[32m┌───▼──────┐   ┌───────────────▼──────┐[m
[32m+[m[32m│  lane    │   │   pothole            │[m
[32m+[m[32m│ detector │   │   detector           │[m
[32m+[m[32m│ (OpenCV) │   │   (YOLOv8-nano)      │[m
[32m+[m[32m│          │   │   ROI: bottom 55%    │[m
[32m+[m[32m└───┬──────┘   └───────────┬──────────┘[m
[32m+[m[32m    │                      │[m
[32m+[m[32m    └─────────────┬────────┘[m
                   ▼[m
         temporal_filter.py[m
           (LaneFilter + ConfidenceFilter)[m
[36m@@ -47,7 +47,6 @@[m [mWebcam (720p @ 15 FPS)[m
 | Model | Architecture | Accuracy | Dataset |[m
 |-------|-------------|----------|---------|[m
 | Pothole detector | YOLOv8-nano | **88.4% mAP50** | Mendeley + Pascal VOC dataset (665 images, 80/20 split) |[m
[31m-| Flood classifier | MobileNetV2 | **100% val acc** | Roboflow floods dataset + dry road images |[m
 [m
 Both models are trained via transfer learning from pre-trained ImageNet/COCO weights.[m
 [m
[36m@@ -81,26 +80,6 @@[m [mDownload the dataset from [Mendeley Data](https://data.mendeley.com/datasets/tp9[m
 [m
 Trains for 50 epochs and saves `models/pothole.pt`. To fine-tune further on new data, just place a new zip in `datasets/` and re-run — the script automatically resumes from existing weights.[m
 [m
[31m-#### Flood classifier (MobileNetV2)[m
[31m-[m
[31m-Arrange images as:[m
[31m-```[m
[31m-datasets/flood/[m
[31m-├── train/[m
[31m-│   ├── flooded/[m
[31m-│   └── normal/[m
[31m-└── val/[m
[31m-    ├── flooded/[m
[31m-    └── normal/[m
[31m-```[m
[31m-[m
[31m-Then run:[m
[31m-```bash[m
[31m-.venv/bin/python scripts/train_flood.py[m
[31m-```[m
[31m-[m
[31m-Saves `models/flood.pth`. The script will auto-scrape images if the folder is missing.[m
[31m-[m
 ### 3. Run the pipeline[m
 [m
 ```bash[m
[36m@@ -126,8 +105,6 @@[m [mAll tunable parameters live in `config.py`:[m
 | `CAMERA_FPS` | `15` | Target capture FPS |[m
 | `POTHOLE_CONF_THRESHOLD` | `0.60` | Min YOLO confidence to report |[m
 | `POTHOLE_EVERY_N_FRAMES` | `2` | Run pothole detection every N frames |[m
[31m-| `FLOOD_CONF_THRESHOLD` | `0.75` | Min flood probability to alert |[m
[31m-| `FLOOD_EVERY_N_FRAMES` | `5` | Run flood detection every N frames |[m
 | `SERIAL_PORT` | `/dev/ttyUSB0` | ESP32 serial port (update to your device) |[m
 | `SERIAL_ENABLED` | `False` | Set `True` when ESP32 is connected |[m
 [m
[36m@@ -138,15 +115,14 @@[m [mAll tunable parameters live in `config.py`:[m
 One JSON object per line at **115200 baud**:[m
 [m
 ```json[m
[31m-{"lane":"MIDDLE","pothole":0.91,"flood":0.0,"alert":"POTHOLE","ts":1724256000}[m
[32m+[m[32m{"lane":"MIDDLE","pothole":0.91,"alert":"POTHOLE","ts":1724256000}[m
 ```[m
 [m
 | Field | Values |[m
 |-------|--------|[m
 | `lane` | `LEFT` \| `MIDDLE` \| `RIGHT` \| `ONE-WAY` \| `UNKNOWN` \| `UNRELIABLE` |[m
 | `pothole` | `0.0` (none) or EMA confidence when confirmed |[m
[31m-| `flood` | `0.0` (none) or EMA confidence when confirmed |[m
[31m-| `alert` | `NONE` \| `POTHOLE` \| `FLOOD` \| `BOTH` |[m
[32m+[m[32m| `alert` | `NONE` \| `POTHOLE` |[m
 | `ts` | Unix timestamp |[m
 [m
 ---[m
[36m@@ -168,17 +144,14 @@[m [mroad_safety/[m
 ├── camera.py                # Webcam capture[m
 ├── lane_detector.py         # Classical CV lane detection[m
 ├── pothole_detector.py      # YOLOv8-nano inference (ROI-cropped)[m
[31m-├── flood_detector.py        # MobileNetV2 binary classifier[m
 ├── temporal_filter.py       # Rolling-window EMA smoothers[m
[31m-├── decision_engine.py       # Multi-modal fusion + alert logic[m
[32m+[m[32m├── decision_engine.py       # Lane and pothole alert logic[m
 ├── serial_esp32.py          # JSON-over-serial ESP32 bridge[m
 ├── requirements.txt[m
 ├── scripts/[m
 │   ├── train_pothole.py     # Fine-tune pothole detector (YOLOv8)[m
[31m-│   └── train_flood.py       # Train flood classifier (MobileNetV2)[m
 ├── models/                  # Trained weights (git-ignored)[m
 │   ├── pothole.pt           # YOLOv8-nano fine-tuned weights[m
[31m-│   └── flood.pth            # MobileNetV2 fine-tuned weights[m
 ├── datasets/                # Training data (git-ignored)[m
 ├── recordings/              # Saved frames/video (git-ignored)[m
 └── tests/[m
[36m@@ -193,9 +166,8 @@[m [mroad_safety/[m
 [m
 | Module | Cadence | Effective FPS |[m
 |--------|---------|--------------|[m
[31m-| Camera capture | Every frame | 15 FPS |[m
[31m-| Lane detection (CV) | Every frame | 15 FPS |[m
[31m-| Pothole detection (YOLOv8) | Every 2nd frame | ~7.5 FPS |[m
[31m-| Flood detection (MobileNetV2) | Every 5th frame | ~3 FPS |[m
[32m+[m[32m| Camera capture | Every frame | 15 FPS requested; actual rate depends on loop throughput |[m
[32m+[m[32m| Lane detection (CV) | Every 2nd frame | Half of the effective pipeline FPS |[m
[32m+[m[32m| Pothole detection (YOLOv8) | Every 3rd frame | One third of the effective pipeline FPS |[m
 [m
[31m-> **Note:** Pothole detection processes only the **bottom 55% of the frame** (road surface area), approximately halving inference time with no accuracy loss.[m
[32m+[m[32m> **Note:** Pothole detection processes only the **bottom 55% of the frame** (road surface area). The HUD FPS is measured from successful camera reads, so it is the actual end-to-end loop rate, not the camera's requested setting.[m
[1mdiff --git a/road_safety/camera.py b/road_safety/camera.